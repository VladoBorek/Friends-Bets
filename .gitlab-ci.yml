workflow:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUESTS'
      when: never
    - if: '$CI_COMMIT_BRANCH'

variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
    DOCKER_BUILDKIT: "1"
    COMPOSE_DOCKER_CLI_BUILD: "1"
    DOCKER_DRIVER: overlay2

default:
  image: docker:27-cli
  services:
    - name: docker:27-dind
      alias: docker
  tags:
    - shared-fi
  before_script:
    - docker version
    - docker compose version
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
    - docker pull "$CI_REGISTRY_IMAGE/dev:cache" || true
    - docker build
        --cache-from "$CI_REGISTRY_IMAGE/dev:cache"
        --build-arg BUILDKIT_INLINE_CACHE=1
        -t pb138-project-dev
        -t "$CI_REGISTRY_IMAGE/dev:cache"
        -f Dockerfile.dev .
    - docker push "$CI_REGISTRY_IMAGE/dev:cache"

stages:
  - test
  - build
  - deploy

verify:
  stage: test
  script:
    - docker compose --profile tools run --rm app npm run lint:local
    - docker compose --profile tools run --rm app npm run test:local
  after_script:
    - docker compose down --remove-orphans

build:
  stage: build
  script:
    - docker compose --profile tools run --rm app npm run generate:local
    - docker compose --profile tools run --rm app npm run build:local
  artifacts:
    paths:
      - dist/
      - client/src/api/gen/
      - client/src/routeTree.gen.ts
    expire_in: 1 week
  after_script:
    - docker compose down --remove-orphans

integration-tests: # TODO
  stage: test
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
  script:
    - echo "TODO"

e2e-tests: # TODO
  stage: test
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
  script:
    - echo "TODO"

deploy: # TODO
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
  script:
    - echo "TODO"
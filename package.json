{
  "name": "backend-setup",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "client": "npm --prefix client run dev",
    "build": "npm --prefix client run build && npm --prefix server run build",
    "lint": "npm --prefix client run lint && npm --prefix server run lint && eslint scripts",
    "preview": "npm --prefix client run preview",
    "api:generate": "npm --prefix client run api:generate",
    "db:generate": "npm --prefix server run db:generate",
    "db:migrate": "npm --prefix server run db:migrate",
    "db:seed": "npm --prefix server run db:seed",
    "cli:query": "npm --prefix server run cli:query",
    "setup": "tsx scripts/setup.ts",
    "server": "npm --prefix server run start"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/node": "^25.5.0",
    "eslint": "^9.39.4",
    "globals": "^17.4.0",
    "tsx": "^4.20.6",
    "typescript-eslint": "^8.56.1"
  }
}
